# Local Vite fallback only. Production values are injected by the ECS container
# into /runtime-config.js; Cognito IDs are public configuration, never secrets.
VITE_API_BASE_URL=/api
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_WEB_CLIENT_ID=
