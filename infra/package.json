{
  "name": "@wally/infra",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "check": "tsc --noEmit -p tsconfig.json",
    "synth": "cdk synth -c environment=staging",
    "deploy:staging": "cdk deploy -c environment=staging --require-approval broadening",
    "deploy:production": "cdk deploy -c environment=production --require-approval broadening"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.197.0",
    "constructs": "^10.4.2",
    "esbuild": "^0.25.5",
    "source-map-support": "^0.5.21"
  },
  "devDependencies": {
    "@types/node": "^22.15.29",
    "aws-cdk": "^2.1019.2",
    "tsx": "^4.19.4"
  }
}
